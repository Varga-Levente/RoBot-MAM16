# ── WebRTC stream ──────────────────────────────────────────────────────────
aiortc>=1.6.0
aiohttp>=3.9.0
av>=11.0.0             # PyAV — video frame kezelés (aiortc függőség)

# ── Gépi látás ─────────────────────────────────────────────────────────────
# Jetson Nano-n a cv2 rendszer szinten telepítve van a JetPack-kel (4.5.0+).
# A venv-et --system-site-packages kapcsolóval kell létrehozni:
#   python3.8 -m venv venv --system-site-packages
# opencv-python-headless NEM szerepel itt — a system cv2-t használjuk.
numpy>=1.24.0

# ── Titkosítás ─────────────────────────────────────────────────────────────
pycryptodome>=3.20.0

# ── OLED kijelző ───────────────────────────────────────────────────────────
luma.oled>=3.13.0
Pillow>=10.0.0

# ── GPIO / Hardware interfészek (Jetson Nano) ──────────────────────────────
# Megjegyzés: Jetson.GPIO előre telepítve van a JetPack-kel.
# Ha manuálisan kell:
# Jetson.GPIO>=2.1.6

# ── I2C (OLED) ────────────────────────────────────────────────────────────
# Megjegyzés: luma.oled I2C-t használ, spidev/adafruit már nem szükséges.

# ── Soros port (IR LED vezérlés + LoRa E22 UART) ──────────────────────────
pyserial>=3.5
