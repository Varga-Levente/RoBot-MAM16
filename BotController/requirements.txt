# ── Gamepad olvasás ───────────────────────────────────────────────────────────
evdev>=1.6.0

# ── Titkosítás (azonos a robot oldalával) ─────────────────────────────────────
pycryptodome>=3.20.0

# ── LoRa (E22-900T22D-V2 UART) + GPIO (M0/M1/AUX) ────────────────────────────
pyserial>=3.5
RPi.GPIO>=0.7.1

# ── Web UI szerver ────────────────────────────────────────────────────────────
aiohttp>=3.9.0
