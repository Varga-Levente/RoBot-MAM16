# Jetson Nano — Python 3.8 (deadsnakes PPA)
# Telepítés:
#   python3.8 -m pip install --ignore-installed numpy
#   python3.8 -m pip install -r requirements-jetson.txt

# ── WebRTC stream ──────────────────────────────────────────────────────────
aiortc>=1.6.0
aiohttp>=3.9.0
av>=11.0.0

# ── Gépi látás ─────────────────────────────────────────────────────────────
opencv-python-headless>=4.5.0,<4.10.0
numpy>=1.21.0,<1.25.0
Pillow>=9.0.0,<11.0.0

# ── Titkosítás ─────────────────────────────────────────────────────────────
pycryptodome>=3.20.0

# ── OLED kijelző ───────────────────────────────────────────────────────────
luma.oled>=3.13.0

# ── SPI / I2C (LoRa + OLED) ───────────────────────────────────────────────
spidev>=3.6
adafruit-blinka>=8.0.0
adafruit-circuitpython-rfm9x>=2.4.0

# ── Soros port (IR LED vezérlés) ───────────────────────────────────────────
pyserial>=3.5

# ── GPIO (Jetson Nano) ─────────────────────────────────────────────────────
# Jetson.GPIO a JetPack-kel előre telepítve van, de ha manuálisan kell:
# Jetson.GPIO>=2.1.6
